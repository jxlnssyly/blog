 <h3 class="ph">
      <p>点击<span>排行</span></p>
    </h3>
    <ul class="paih">

      <% higher_visit.each do |article| %>
        <li>
          <%= link_to article.title, show_path(:id => article.id), title: article.title  %>

        </li>
      <% end -%>
</ul>
